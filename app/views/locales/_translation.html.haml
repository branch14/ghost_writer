- id = "translation-#{translation.id}" 
%li.translation{ :id => id, :class => "#{translation.active? ? 'active' : 'inactive'}" }
  .token
    %span.counter=translation.miss_counter
    &mdash;
    %span.raw=translation.raw
    -# .tags
    -#   -if translation.tags.size > 0
    -#     -translation.tags.each do |tag|
    -#       -#=link_to tag.name, { :action => :tag, :id => tag.name }, :class => 'css_class'
    -#       =tag.name
    -#   -else
    -#     %i=t(:no_tags)
    -#   =text_field_tag "#{id}-tags", translation.tag_list, :class => 'hidden'
    -#   %span.edit=link_to_function t(:edit), "editTagList('#{id}')"
    &mdash;
    %span.actions
      -#!=link_to t(:edit), edit_project_token_path(@project, translation.token)
      -#!=link_to t(:delete), project_token_path(@project, translation.token), :method => 'delete', :confirm => t('.delete.sure', :key => translation.raw)
  .content=best_in_place translation, :content   
