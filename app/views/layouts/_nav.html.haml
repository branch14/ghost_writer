.right
  %ul.nav
    - if user_signed_in?
      - if current_user.projects.count > 1
        %li!= link_to t(:my_projects), projects_path
      - else
        %li!= link_to t(:new_project), new_project_path
      %li!= link_to t(:logout), destroy_user_session_path
    - else
      %li!= link_to t(:login), new_user_session_path
