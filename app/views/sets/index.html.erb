<h1>Listing sets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sets.each do |set| %>
  <tr>
    <td><%= link_to 'Show', set %></td>
    <td><%= link_to 'Edit', edit_set_path(set) %></td>
    <td><%= link_to 'Destroy', set, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Set', new_set_path %>
